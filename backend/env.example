# Database
DATABASE_URL="postgresql://user:password@localhost:5432/barrios?schema=public"

# JWT
JWT_SECRET="change-this-to-a-random-secret-in-production"

# Server
PORT=3001
NODE_ENV=development
FRONTEND_URL="http://localhost:3000"

# WhatsApp (configurar según proveedor)
WHATSAPP_API_KEY=""
WHATSAPP_API_URL=""
WHATSAPP_WEBHOOK_SECRET=""

# Email
EMAIL_SMTP_HOST="smtp.gmail.com"
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USER=""
EMAIL_SMTP_PASS=""
EMAIL_FROM="noreply@barrios.com"

# Storage (para comprobantes)
STORAGE_TYPE="local" # local, s3, etc.
STORAGE_PATH="./uploads"
